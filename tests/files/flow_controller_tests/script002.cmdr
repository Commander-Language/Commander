// test functions, math operations, and calls
add(x, y) {
    return x + y;
};
sub(x, y) {
    return x - y;
};
mul(x, y) {
    return x * y;
};
div(x, y) {
    return x / y;
};

x = 10;
y = 5;

sum = add(x, y);
difference = sub(x, y);
product = mul(x, y);
quotient = div(x, y);
